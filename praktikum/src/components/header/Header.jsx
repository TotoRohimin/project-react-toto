function Header() {
  return (
    <p>
      Below is an example form but entilery with Bootstrap's form controls. Each required form group has a<br />
      validation state that can be triggered by attemping to submit the form without completing it.
    </p>
  );
}

export default Header;
